# Firmware

Empty for now. Reserved for any microcontroller firmware (Arduino, STM32,
RP2040) you might offload real-time tasks to later — for example a dedicated
MCU handling step-pulse generation while the Pi runs ROS 2.
